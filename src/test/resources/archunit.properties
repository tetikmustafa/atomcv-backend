# The module packages are still empty, so every rule would otherwise fail with
# "failed to check any classes". Revisit once the modules carry real classes:
# turning this back on catches rules that silently stop matching after a
# package rename.
archRule.failOnEmptyShould=false
