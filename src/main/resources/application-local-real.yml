# Real calls, nothing saved. For working on a prompt, where every
# run should differ and no fixture should be frozen from a draft.
atomcv:
  llm:
    fake:
      synthesize: false

logging:
  level:
    com.mustafatetik.atomcv.llm: debug
