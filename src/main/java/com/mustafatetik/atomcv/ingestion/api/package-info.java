/**
 * Where a CV comes in from.
 *
 * <p>One endpoint so far, and it is deliberately thin: everything it can decide
 * about a file it decides through {@code ingestion.service}, and everything it
 * cannot is a job.
 */
package com.mustafatetik.atomcv.ingestion.api;
