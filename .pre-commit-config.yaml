# Secret scanning before anything reaches a commit (Adim 1.4).
# CI runs gitleaks over full history as well; this hook is the local first line.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
